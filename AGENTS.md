# rness workspace member — scope: rness

<!-- BEGIN rness -->
<!-- Managed by `rness sync` (spec 0001). Until the CLI ships, maintained by hand. -->
<!-- scope: rness · contract: 1 -->

This repository is scope `rness` of an rness workspace. Canonical context lives in
`../../.rness/` — open the workspace for full access. If `.rness/` is not
reachable, this repository was cloned standalone: you have this block only.

Read `../../.rness/STATUS.md`, then task-relevant `../../.rness/adr/`,
`../../.rness/specs/`, `../../.rness/standards/` (global + `standards/rness/`).
Or run `rness context --scope rness`.

<!-- END rness -->
